#include <bits/stdc++.h>
using namespace std;

int main(void) {
	// 아래의 두 줄은 입출력 시간초과 최적화 하기 위해 사용
	ios::sync_with_stdio(false);	// C++과 C의 표준 스트림 동기화 해제
	cin.tie(nullptr);	// cin 버퍼 비우기 해제

	int n;
	cin >> n;

	vector<int> v(n);
	for (auto &i : v) {
		cin >> i;
	}



	return 0;
}